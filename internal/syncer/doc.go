// Package syncer implements the Fork_Synchronizer, which updates
// Managed_Repositories from their upstream remote using fast-forward-only
// semantics and optionally pushes updates to the origin remote.
package syncer
